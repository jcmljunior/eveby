/**
 * Responsavel pela verificação das condições de execução do comando.
 * Verifica o numero maximo de parametros aceitos por
 * cada comando, e então permite a execução caso o mesmo
 * esteja de acordo.
 * Verifica o tipo de parametro. (STRING, INT, BOOLEAN,)
 */
export default class GuardManager {}
